# 0. Backend Bootstrap and Tooling Baseline

Status: Not Started  
Mandatory: Yes  
Depends On: None

## Goal

Create the backend project foundation so later tasks can add application code without reworking tooling.

## Confirmed Current Behavior

- `backend/package.json` is still the default placeholder.
- No TypeScript config, Prisma config, source folder, or environment examples exist.
- The backend folder does not yet reflect the required assignment stack.

## Implementation Scope

- Initialize the backend package for Node.js + TypeScript + Express.
- Add the core runtime and dev dependencies defined in `backend/backend.AGENTS.md`.
- Add the baseline scripts for local development, build, typecheck, Prisma generation, migrations, seeding, and tests.
- Create the top-level backend folder structure so later tasks can add files in predictable locations.
- Keep the setup minimal and assignment-scoped; do not add unnecessary infrastructure.

## Files Expected To Change

- `backend/package.json`
- `backend/tsconfig.json`
- `backend/.gitignore`
- `backend/prisma/`
- `backend/src/`
- `backend/tests/`

## Acceptance Criteria

- `backend/` has a real TypeScript/Express package layout instead of a placeholder package.
- Script names align with the backend instructions and future tasks can build on them directly.
- The directory structure is present for config, controllers, services, routes, middleware, schemas, utils, types, and Prisma.
- No application business logic is implemented yet beyond the structural baseline.

## Verification

- `npm install`
- `npm run typecheck`
- `npm run build`

## Completion Evidence

Status: Not Started

Implemented files:

Typecheck command:

Typecheck result: Not Run

Build command:

Build result: Not Run

Backend test command:

Backend test result: Not Run

Manual verification:

Evidence:

Caveats:
